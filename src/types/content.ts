import type { CollectionEntry } from 'astro:content';

export type ValueProp = CollectionEntry<'value-props'>;
export type UseCase = CollectionEntry<'use-cases'>;
export type Fact = CollectionEntry<'facts'>;
